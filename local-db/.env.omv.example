# Copy to ".env" beside the OMV compose stack and fill in your key.
# Used by the local-db server for the natural-language (Claude) query feature.
# The app still runs without it — that feature is just disabled.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
